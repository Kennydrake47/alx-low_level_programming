#include "main.h"
/**
 * print_alphabet - print all alphabelt in lowercase
 * Return: Always 0
*/
void print_alphabet(void);
{
char latter;
for (latter = 'a'; latter <= 'z'; latter++)
_putchar(latter);
_putchar('\n');
return (0);
}
